/** @type {import('tailwindcss').Config} */
module.exports = {
  theme: {
    extend: {
      fontFamily: {
        serif: ['var(--font-ballaghi)', 'serif'],
        sans: ['var(--font-satoshi)', 'sans-serif'],
      },
    },
  },
};
